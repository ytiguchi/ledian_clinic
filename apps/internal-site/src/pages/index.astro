---
import Layout from '../components/Layout.astro';
---

<Layout>
  <div class="space-y-6">
    <!-- ヘッダー -->
    <div class="flex justify-between items-center">
      <div>
        <h1 class="text-4xl font-bold">ダッシュボード</h1>
        <p class="text-gray-600 mt-2">レディアンクリニック 管理サイト</p>
      </div>
      <div class="text-sm text-gray-500">
        <span id="currentDate"></span>
      </div>
    </div>

    <!-- 統計カード -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6" id="statsGrid">
      <div class="bg-white rounded-lg shadow p-6">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600">カテゴリ数</p>
            <p class="text-3xl font-bold mt-2" id="categoriesCount">-</p>
          </div>
          <div class="text-4xl">📂</div>
        </div>
      </div>
      
      <div class="bg-white rounded-lg shadow p-6">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600">施術数</p>
            <p class="text-3xl font-bold mt-2" id="treatmentsCount">-</p>
          </div>
          <div class="text-4xl">✨</div>
        </div>
      </div>
      
      <div class="bg-white rounded-lg shadow p-6">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600">料金プラン数</p>
            <p class="text-3xl font-bold mt-2" id="plansCount">-</p>
          </div>
          <div class="text-4xl">💰</div>
        </div>
      </div>
      
      <div class="bg-white rounded-lg shadow p-6">
        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-gray-600">アクティブなキャンペーン</p>
            <p class="text-3xl font-bold mt-2" id="campaignsCount">-</p>
          </div>
          <div class="text-4xl">🎁</div>
        </div>
      </div>
    </div>

    <!-- クイックアクション -->
    <div class="bg-white rounded-lg shadow p-6">
      <h2 class="text-2xl font-semibold mb-4">クイックアクション</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
        <a href="/pricing" class="block p-6 border-2 border-accent rounded-lg hover:bg-accent hover:text-white transition text-center group">
          <span class="text-4xl mb-3 block group-hover:scale-110 transition-transform">💰</span>
          <span class="font-semibold text-lg block">料金管理</span>
          <span class="text-sm text-gray-600 group-hover:text-white mt-1 block">メニュー料金の管理</span>
        </a>
        
        <a href="/campaigns" class="block p-6 border-2 border-accent rounded-lg hover:bg-accent hover:text-white transition text-center group">
          <span class="text-4xl mb-3 block group-hover:scale-110 transition-transform">🎁</span>
          <span class="font-semibold text-lg block">キャンペーン</span>
          <span class="text-sm text-gray-600 group-hover:text-white mt-1 block">キャンペーン管理</span>
        </a>
        
        <a href="/treatments" class="block p-6 border-2 border-accent rounded-lg hover:bg-accent hover:text-white transition text-center group">
          <span class="text-4xl mb-3 block group-hover:scale-110 transition-transform">✨</span>
          <span class="font-semibold text-lg block">施術管理</span>
          <span class="text-sm text-gray-600 group-hover:text-white mt-1 block">施術情報の管理</span>
        </a>
        
        <a href="/before-afters" class="block p-6 border-2 border-accent rounded-lg hover:bg-accent hover:text-white transition text-center group">
          <span class="text-4xl mb-3 block group-hover:scale-110 transition-transform">📸</span>
          <span class="font-semibold text-lg block">症例写真</span>
          <span class="text-sm text-gray-600 group-hover:text-white mt-1 block">ビフォーアフター管理</span>
        </a>
      </div>
    </div>

    <!-- アクティブなキャンペーン -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-2xl font-semibold">アクティブなキャンペーン</h2>
        <a href="/campaigns" class="text-accent hover:underline text-sm">すべて見る →</a>
      </div>
      <div id="activeCampaigns" class="space-y-3">
        <div class="text-center py-8 text-gray-500">
          <p>読み込み中...</p>
        </div>
      </div>
    </div>

    <!-- 最近追加された料金プラン -->
    <div class="bg-white rounded-lg shadow p-6">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-2xl font-semibold">最近追加された料金プラン</h2>
        <a href="/pricing" class="text-accent hover:underline text-sm">すべて見る →</a>
      </div>
      <div id="recentPlans" class="space-y-3">
        <div class="text-center py-8 text-gray-500">
          <p>読み込み中...</p>
        </div>
      </div>
    </div>
  </div>
</Layout>

<script>
  // 現在の日付を表示
  const now = new Date();
  const options: Intl.DateTimeFormatOptions = { 
    year: 'numeric', 
    month: 'long', 
    day: 'numeric',
    weekday: 'long'
  };
  const dateStr = now.toLocaleDateString('ja-JP', options);
  const dateElement = document.getElementById('currentDate');
  if (dateElement) {
    dateElement.textContent = dateStr;
  }

  // 統計情報を読み込む
  async function loadStats() {
    try {
      // カテゴリ数
      const catRes = await fetch('/api/categories');
      if (catRes.ok) {
        const catData = await catRes.json();
        const categoriesCount = catData.categories?.length || 0;
        const catElement = document.getElementById('categoriesCount');
        if (catElement) catElement.textContent = categoriesCount.toString();
      }

      // 料金プラン数（これで施術数も概算できる）
      const pricingRes = await fetch('/api/pricing');
      if (pricingRes.ok) {
        const pricingData = await pricingRes.json();
        const plans = pricingData.plans || [];
        const plansCount = plans.length;
        const plansElement = document.getElementById('plansCount');
        if (plansElement) plansElement.textContent = plansCount.toString();

        // 施術数（ユニークなtreatment_idの数）
        const uniqueTreatments = new Set(plans.map((p: any) => p.treatment_id));
        const treatmentsCount = uniqueTreatments.size;
        const treatmentsElement = document.getElementById('treatmentsCount');
        if (treatmentsElement) treatmentsElement.textContent = treatmentsCount.toString();
      }

      // アクティブなキャンペーン数
      const campaignsRes = await fetch('/api/campaigns?is_active=true');
      if (campaignsRes.ok) {
        const campaignsData = await campaignsRes.json();
        const campaignsCount = campaignsData.campaigns?.length || 0;
        const campaignsElement = document.getElementById('campaignsCount');
        if (campaignsElement) campaignsElement.textContent = campaignsCount.toString();
      }
    } catch (error) {
      console.error('統計情報読み込みエラー:', error);
    }
  }

  // アクティブなキャンペーンを読み込む
  async function loadActiveCampaigns() {
    try {
      const res = await fetch('/api/campaigns?is_active=true');
      if (!res.ok) return;

      const data = await res.json();
      const campaigns = data.campaigns || [];
      const container = document.getElementById('activeCampaigns');
      if (!container) return;

      if (campaigns.length === 0) {
        container.innerHTML = '<p class="text-center py-8 text-gray-500">アクティブなキャンペーンはありません</p>';
        return;
      }

      // 最新5件のみ表示
      const recentCampaigns = campaigns.slice(0, 5);

      container.innerHTML = recentCampaigns.map((campaign: any) => {
        const startDate = campaign.start_date ? new Date(campaign.start_date).toLocaleDateString('ja-JP') : '開始日未設定';
        const endDate = campaign.end_date ? new Date(campaign.end_date).toLocaleDateString('ja-JP') : '終了日未設定';
        
        return `
          <div class="border rounded-lg p-4 hover:bg-gray-50 transition">
            <div class="flex justify-between items-start">
              <div class="flex-1">
                <div class="font-semibold text-lg">${escapeHtml(campaign.title)}</div>
                <div class="text-sm text-gray-600 mt-1">${startDate} 〜 ${endDate}</div>
                ${campaign.description ? `<div class="text-sm text-gray-500 mt-2">${escapeHtml(campaign.description.substring(0, 100))}${campaign.description.length > 100 ? '...' : ''}</div>` : ''}
              </div>
              <a href="/campaigns/${campaign.id}/edit" class="ml-4 px-4 py-2 bg-accent text-white rounded-lg hover:bg-accent-light transition text-sm whitespace-nowrap">
                編集
              </a>
            </div>
          </div>
        `;
      }).join('');
    } catch (error) {
      console.error('キャンペーン読み込みエラー:', error);
      const container = document.getElementById('activeCampaigns');
      if (container) {
        container.innerHTML = '<p class="text-center py-8 text-red-500">読み込みに失敗しました</p>';
      }
    }
  }

  // 最近追加された料金プランを読み込む
  async function loadRecentPlans() {
    try {
      const res = await fetch('/api/pricing');
      if (!res.ok) return;

      const data = await res.json();
      const plans = data.plans || [];
      const container = document.getElementById('recentPlans');
      if (!container) return;

      if (plans.length === 0) {
        container.innerHTML = '<p class="text-center py-8 text-gray-500">料金プランがありません</p>';
        return;
      }

      // 最新10件を表示（created_atでソートする必要があるが、APIが対応していない場合はID順）
      const recentPlans = plans.slice(0, 10);

      container.innerHTML = recentPlans.map((plan: any) => {
        return `
          <div class="border rounded-lg p-4 hover:bg-gray-50 transition">
            <div class="flex justify-between items-start">
              <div class="flex-1">
                <div class="font-semibold">${escapeHtml(plan.treatment_name)} - ${escapeHtml(plan.plan_name)}</div>
                <div class="text-sm text-gray-600 mt-1">${escapeHtml(plan.category_name)} / ${escapeHtml(plan.subcategory_name)}</div>
                <div class="text-lg font-semibold text-accent mt-2">¥${plan.price_taxed.toLocaleString()}（税込）</div>
              </div>
              <a href="/pricing/${plan.id}/edit" class="ml-4 px-4 py-2 bg-accent text-white rounded-lg hover:bg-accent-light transition text-sm whitespace-nowrap">
                編集
              </a>
            </div>
          </div>
        `;
      }).join('');
    } catch (error) {
      console.error('料金プラン読み込みエラー:', error);
      const container = document.getElementById('recentPlans');
      if (container) {
        container.innerHTML = '<p class="text-center py-8 text-red-500">読み込みに失敗しました</p>';
      }
    }
  }

  function escapeHtml(text: string): string {
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
  }

  // ページ読み込み時に実行
  document.addEventListener('DOMContentLoaded', () => {
    loadStats();
    loadActiveCampaigns();
    loadRecentPlans();
  });
</script>
