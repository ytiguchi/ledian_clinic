---
import Layout from '../../components/Layout.astro';

const { slug } = Astro.params;
---

<Layout>
  <div class="education-page">
    <!-- Breadcrumb -->
    <div class="breadcrumb">
      <a href="/treatments" class="breadcrumb-link">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M19 12H5M12 19l-7-7 7-7"/>
        </svg>
        施術管理に戻る
      </a>
    </div>

    <!-- Loading State -->
    <div id="loadingState" class="loading-container">
      <div class="loading">教育コンテンツを読み込み中...</div>
    </div>

    <!-- Content Container -->
    <div id="contentContainer" style="display: none;">
      <!-- Header -->
      <div class="education-header">
        <div class="header-info">
          <span class="header-label" id="categoryLabel"></span>
          <h1 class="header-title" id="treatmentName"></h1>
          <p class="header-subtitle" id="treatmentNameEn"></p>
        </div>
        <a id="sourceLink" href="#" target="_blank" class="source-link">
          公式サイトで見る
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6M15 3h6v6M10 14L21 3"/>
          </svg>
        </a>
      </div>

      <!-- About Section -->
      <section class="content-section" id="aboutSection">
        <div class="section-header">
          <h2 class="section-title">
            <span class="section-icon">📝</span>
            施術について
          </h2>
        </div>
        <div class="section-body">
          <p class="about-text" id="aboutText"></p>
        </div>
      </section>

      <!-- Features Section -->
      <section class="content-section" id="featuresSection">
        <div class="section-header">
          <h2 class="section-title">
            <span class="section-icon">✨</span>
            特徴・こだわり
          </h2>
        </div>
        <div class="section-body">
          <div class="features-grid" id="featuresGrid"></div>
        </div>
      </section>

      <!-- Overview Section -->
      <section class="content-section" id="overviewSection">
        <div class="section-header">
          <h2 class="section-title">
            <span class="section-icon">📋</span>
            施術概要
          </h2>
        </div>
        <div class="section-body">
          <div class="overview-table" id="overviewTable"></div>
        </div>
      </section>

      <!-- FAQ Section -->
      <section class="content-section" id="faqSection">
        <div class="section-header">
          <h2 class="section-title">
            <span class="section-icon">❓</span>
            よくある質問（スタッフ学習用）
          </h2>
        </div>
        <div class="section-body">
          <div class="faq-list" id="faqList"></div>
        </div>
      </section>

      <!-- Training Tips -->
      <section class="content-section training-section">
        <div class="section-header">
          <h2 class="section-title">
            <span class="section-icon">💡</span>
            カウンセリングポイント
          </h2>
        </div>
        <div class="section-body">
          <div class="training-tips">
            <div class="tip-card">
              <h4>患者様への説明ポイント</h4>
              <ul>
                <li>施術の効果とメリットを分かりやすく説明</li>
                <li>ダウンタイムや副作用についても正直に伝える</li>
                <li>期待値を適切に設定（過度な期待を避ける）</li>
              </ul>
            </div>
            <div class="tip-card">
              <h4>よくある質問への対応</h4>
              <ul>
                <li>FAQの内容を理解し、自分の言葉で説明できるようにする</li>
                <li>不明点は医師に確認することを忘れずに</li>
              </ul>
            </div>
            <div class="tip-card">
              <h4>クロスセル・アップセル</h4>
              <ul>
                <li>関連施術との組み合わせ提案を検討</li>
                <li>コース契約のメリットを説明</li>
              </ul>
            </div>
          </div>
        </div>
      </section>
    </div>

    <!-- Not Found State -->
    <div id="notFoundState" style="display: none;" class="not-found">
      <div class="not-found-icon">📭</div>
      <h2>教育コンテンツが見つかりません</h2>
      <p>この施術の教育コンテンツはまだ登録されていません。</p>
      <a href="/treatments" class="btn btn-primary">施術一覧に戻る</a>
    </div>
  </div>
</Layout>

<style>
  .education-page {
    max-width: 900px;
    margin: 0 auto;
  }

  /* Breadcrumb */
  .breadcrumb {
    margin-bottom: 1.5rem;
  }

  .breadcrumb-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-text-secondary);
    text-decoration: none;
    font-size: 0.875rem;
    transition: color var(--transition-fast);
  }

  .breadcrumb-link:hover {
    color: var(--color-accent);
  }

  /* Loading */
  .loading-container {
    padding: 4rem 2rem;
    text-align: center;
  }

  /* Header */
  .education-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 2rem;
    padding: 2rem;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    border-radius: var(--radius-lg);
    color: white;
    margin-bottom: 2rem;
  }

  .header-label {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background: var(--color-accent);
    color: var(--color-primary);
    font-size: 0.75rem;
    font-weight: 600;
    border-radius: var(--radius-full);
    margin-bottom: 0.5rem;
  }

  .header-title {
    font-size: 2rem;
    font-weight: 700;
    margin: 0 0 0.25rem 0;
  }

  .header-subtitle {
    font-size: 1rem;
    opacity: 0.8;
    margin: 0;
    font-style: italic;
  }

  .source-link {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.5rem 1rem;
    background: rgba(255, 255, 255, 0.15);
    color: white;
    text-decoration: none;
    font-size: 0.8125rem;
    border-radius: var(--radius-md);
    transition: background var(--transition-fast);
    white-space: nowrap;
  }

  .source-link:hover {
    background: rgba(255, 255, 255, 0.25);
  }

  /* Sections */
  .content-section {
    background: var(--color-bg-card);
    border-radius: var(--radius-lg);
    border: 1px solid var(--color-border-light);
    margin-bottom: 1.5rem;
    overflow: hidden;
  }

  .section-header {
    padding: 1.25rem 1.5rem;
    background: var(--color-bg-hover);
    border-bottom: 1px solid var(--color-border-light);
  }

  .section-title {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.125rem;
    font-weight: 600;
    margin: 0;
    color: var(--color-text);
  }

  .section-icon {
    font-size: 1.25rem;
  }

  .section-body {
    padding: 1.5rem;
  }

  /* About */
  .about-text {
    font-size: 1rem;
    line-height: 1.8;
    color: var(--color-text);
    margin: 0;
  }

  /* Features */
  .features-grid {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .feature-card {
    padding: 1.25rem;
    background: var(--color-bg-hover);
    border-radius: var(--radius-md);
    border-left: 4px solid var(--color-accent);
  }

  .feature-title {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 0 0 0.5rem 0;
  }

  .feature-desc {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin: 0;
  }

  /* Overview Table */
  .overview-table {
    display: grid;
    gap: 0.5rem;
  }

  .overview-row {
    display: grid;
    grid-template-columns: 140px 1fr;
    gap: 1rem;
    padding: 0.75rem 0;
    border-bottom: 1px solid var(--color-border-light);
  }

  .overview-row:last-child {
    border-bottom: none;
  }

  .overview-label {
    font-weight: 600;
    color: var(--color-text);
    font-size: 0.875rem;
  }

  .overview-value {
    color: var(--color-text-secondary);
    font-size: 0.875rem;
  }

  /* FAQ */
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .faq-item {
    padding: 1.25rem;
    background: var(--color-bg-hover);
    border-radius: var(--radius-md);
  }

  .faq-question {
    font-size: 1rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 0 0 0.75rem 0;
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
  }

  .faq-question::before {
    content: 'Q.';
    color: var(--color-accent);
    font-weight: 700;
  }

  .faq-answer {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin: 0;
    padding-left: 1.5rem;
  }

  .faq-answer::before {
    content: 'A.';
    color: var(--color-primary);
    font-weight: 600;
    margin-right: 0.5rem;
  }

  /* Training Tips */
  .training-section {
    background: linear-gradient(135deg, var(--color-accent-subtle) 0%, var(--color-bg-card) 100%);
  }

  .training-tips {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }

  .tip-card {
    padding: 1.25rem;
    background: var(--color-bg-card);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
  }

  .tip-card h4 {
    font-size: 0.9375rem;
    font-weight: 600;
    color: var(--color-text);
    margin: 0 0 0.75rem 0;
  }

  .tip-card ul {
    margin: 0;
    padding-left: 1.25rem;
  }

  .tip-card li {
    font-size: 0.8125rem;
    color: var(--color-text-secondary);
    margin-bottom: 0.375rem;
    line-height: 1.5;
  }

  /* Not Found */
  .not-found {
    text-align: center;
    padding: 4rem 2rem;
  }

  .not-found-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
  }

  .not-found h2 {
    font-size: 1.5rem;
    margin: 0 0 0.5rem 0;
  }

  .not-found p {
    color: var(--color-text-muted);
    margin: 0 0 1.5rem 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .education-header {
      flex-direction: column;
      gap: 1rem;
    }

    .training-tips {
      grid-template-columns: 1fr;
    }

    .overview-row {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }
  }
</style>

<script define:vars={{ slug }}>
  async function loadEducationContent() {
    const loadingState = document.getElementById('loadingState');
    const contentContainer = document.getElementById('contentContainer');
    const notFoundState = document.getElementById('notFoundState');

    try {
      const res = await fetch(`/api/education/${slug}`);
      
      if (!res.ok) {
        loadingState.style.display = 'none';
        notFoundState.style.display = 'block';
        return;
      }

      const data = await res.json();
      
      if (!data || data.error) {
        loadingState.style.display = 'none';
        notFoundState.style.display = 'block';
        return;
      }

      // Populate content
      document.getElementById('treatmentName').textContent = data.name_ja || '';
      document.getElementById('treatmentNameEn').textContent = data.name_en || '';
      document.getElementById('categoryLabel').textContent = data.category || '施術';
      
      if (data.url) {
        document.getElementById('sourceLink').href = data.url;
      }

      // About
      const aboutSection = document.getElementById('aboutSection');
      if (data.about || data.description) {
        document.getElementById('aboutText').textContent = data.about || data.description;
      } else {
        aboutSection.style.display = 'none';
      }

      // Features
      const featuresSection = document.getElementById('featuresSection');
      const featuresGrid = document.getElementById('featuresGrid');
      if (data.features && data.features.length > 0) {
        featuresGrid.innerHTML = data.features.map(f => `
          <div class="feature-card">
            <h4 class="feature-title">${escapeHtml(f.title)}</h4>
            <p class="feature-desc">${escapeHtml(f.description)}</p>
          </div>
        `).join('');
      } else {
        featuresSection.style.display = 'none';
      }

      // Overview
      const overviewSection = document.getElementById('overviewSection');
      const overviewTable = document.getElementById('overviewTable');
      if (data.overview && Object.keys(data.overview).length > 0) {
        overviewTable.innerHTML = Object.entries(data.overview)
          .filter(([key]) => key !== '項目' && key !== '内容')
          .map(([key, value]) => `
            <div class="overview-row">
              <div class="overview-label">${escapeHtml(key)}</div>
              <div class="overview-value">${escapeHtml(value)}</div>
            </div>
          `).join('');
      } else {
        overviewSection.style.display = 'none';
      }

      // FAQ
      const faqSection = document.getElementById('faqSection');
      const faqList = document.getElementById('faqList');
      if (data.faqs && data.faqs.length > 0) {
        faqList.innerHTML = data.faqs.map(faq => `
          <div class="faq-item">
            <h4 class="faq-question">${escapeHtml(faq.question)}</h4>
            <p class="faq-answer">${escapeHtml(faq.answer)}</p>
          </div>
        `).join('');
      } else {
        faqSection.style.display = 'none';
      }

      loadingState.style.display = 'none';
      contentContainer.style.display = 'block';

    } catch (error) {
      console.error('Error loading education content:', error);
      loadingState.style.display = 'none';
      notFoundState.style.display = 'block';
    }
  }

  function escapeHtml(text) {
    if (!text) return '';
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
  }

  document.addEventListener('DOMContentLoaded', loadEducationContent);
</script>


