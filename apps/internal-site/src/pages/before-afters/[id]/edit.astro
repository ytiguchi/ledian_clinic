---
import Layout from '../../../components/Layout.astro';
import BeforeAfterForm from '../../../components/BeforeAfterForm.astro';

const id = Astro.params.id;
export const prerender = false;
---

<Layout>
  <BeforeAfterForm mode="edit" beforeAfterId={id} />
</Layout>
